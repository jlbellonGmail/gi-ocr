```yaml
status: approved
attempt: 2
feedback:
  - "No bloqueante: el criterio 7 (caso 'documento no reconocido') describe como un hecho garantizado que un fixture con 'mismo tamaño de fuente, contraste y brillo medio' que `gas_valid`/`cevt_valid` va a dar brillo medio 'dentro del rango ok/warn, nunca reject'. Verifiqué el cálculo aproximado: un canvas PIL blanco puro (255,255,255) de 1000x1400 con ~70 caracteres de texto dibujado a fuente 26px cubre bajo el 1% del área total, lo que da un brillo medio estimado ~253-254 — dentro del rango 'warn' pero peligrosamente cerca del umbral `GI_OCR_QUALITY_BRIGHTNESS_BRIGHT_REJECT_ABOVE` (254.5). Si el runner de CI cae al fallback `ImageFont.load_default()` (bitmap mucho más pequeño, menos píxeles negros que una TrueType de 26px), el brillo medio podría subir por encima de 254.5 y voltear el veredicto a `reject` — no solo para el fixture 'no reconocido' nuevo, sino también para los fixtures ya existentes `gas_valid`/`cevt_valid` reutilizados de `scripts/benchmark_captura.py::_base_case_image`, que hasta hoy nunca fueron ejercitados contra `quality_gate.evaluate` con el motor OCR real (confirmé que ni `test_benchmark_captura.py` ni ningún otro test en el repo corre `_base_case_image(...)` a través de `capture_pipeline.process_document` real; los tests existentes de integración `quality_gate`+`process_document` usan fondos gris `bg=210` o una foto real, nunca un canvas blanco 255,255,255). El spec ya reconoce esta clase de riesgo genérico en 'Casos borde' (interacción `quality_gate`/suite) y en 'Riesgos/supuestos' (riesgo de fuente tipográfica), y lo marca explícitamente como 'riesgo de calibración explícito, no bloqueante', delegando la verificación empírica al builder en CI real — lo cual es una postura razonable dado que el propio criterio 7(ii) hace que el test falle de forma explícita y visible si el brillo cruza a `reject`, en vez de fallar en silencio. No bloqueo el spec por esto, pero recomiendo que quede consignado explícitamente en el `test-report` de QA (ligado al criterio 13 sobre tiempo/comportamiento observado) qué brillo medio real dio el fixture 'documento no reconocido' y los fixtures válidos en CI, para dejar evidencia de que el margen respecto de 254.5 es seguro y no un acierto casual del entorno de desarrollo local."
```

## Verificación contra código (attempt 2)

- `backend/app/templates/providers.py`: `classify_keywords` citados en el spec para `LITORAL_GAS` (`"litoral gas"`, `"litoralgas"`, `"litoral"`) y `CEVT` (`"cevt"`, `"cooperativa"`, `"electri"`) coinciden exactamente con `litoral_gas_template()` (línea 73) y `cevt_template()` (línea 127). `unknown_template()` confirma `required_fields=[]`, `fields=[]`, `classify_keywords=[]` tal como cita el spec.
- `backend/app/quality_gate.py`: confirmados los umbrales exactos citados por el spec — `GI_OCR_QUALITY_BLUR_REJECT_BELOW = 40.0` (Laplaciano varianza), `GI_OCR_QUALITY_BRIGHTNESS_BRIGHT_REJECT_ABOVE = 254.5` (brillo medio de gris). El veredicto agregado es efectivamente el peor sub-veredicto entre las señales evaluables (`evaluate()`, líneas 403-408).
- `backend/app/capture_pipeline.py`: confirmado que `process_document` corre `quality_gate.evaluate` (línea 242) y corta a `_quality_rejected_result` (línea 250) si `verdict == "reject"`, cuyo `processing_metadata` (líneas 280-296) **no** tiene la clave `provider_detected` — coincide exactamente con el hallazgo de `audit-1.md`. El camino normal citado como "líneas ~252-271" es preciso: el bloque de `trace`, `image_prep.prepare`, `process_image(...)` y el `return result` ocupa exactamente esas líneas. `provider_detected` se setea en `process_image` (líneas 190 y 381) y por lógica de clasificación (líneas 75-85, 338-352) cae a `"UNKNOWN"` cuando ningún `classify_keyword` matchea — consistente con lo que el criterio 7 exige verificar.
- `backend/tests/test_quality_gate.py`: confirmé que ningún test de integración existente (`test_process_document_reject_does_not_invoke_ocr`, `test_process_document_warn_still_runs_ocr_and_flags_metadata`, `test_process_document_ok_has_neutral_quality_gate_block`) usa un fondo blanco puro como el de `_base_case_image`; usan `bg=210` (textura sintética) o una foto real (`gas_sample.jpg`). Esto confirma que la suposición del criterio 7 sobre el comportamiento de brillo de `gas_valid`/`cevt_valid` frente a `quality_gate` es plausible pero no está empíricamente verificada por ningún test existente — de ahí el punto no bloqueante arriba.
- Revisión de consistencia interna: no quedan referencias residuales al fixture "en blanco" anterior fuera de contexto histórico/decisión (líneas 119, 243-244, 333, 385, 391 de `spec.md`, todas correctamente contextualizadas como descripción de código preexistente o explicación de la decisión frente a `audit-1.md`).
- Criterios 1-5 (docs técnica/usuario, `decision.md`, enlaces de índice) siguen exigidos explícitamente — no rechazo automático.
- Resto de "Alcance", "Contexto", criterios 8-14, "Casos borde" y "Riesgos/supuestos" siguen siendo internamente consistentes tras la reescritura del criterio 7; no se detectó ninguna nueva inconsistencia introducida por el cambio.
- La corrección resuelve completamente el punto bloqueante de `audit-1.md` (fixture cambiado a texto legible sin `classify_keywords`, con verificación explícita de `quality_gate.verdict != "reject"`) y el punto no bloqueante (se agregó el caso borde sobre la interacción `quality_gate`/suite en general, no solo para el caso "no reconocido").

## Archivos relevantes

- `D:\proyectos\gi-ocr\runs\08-regresion-dataset-ocr\spec.md`
- `D:\proyectos\gi-ocr\runs\08-regresion-dataset-ocr\audit-1.md`
- `D:\proyectos\gi-ocr\backend\app\capture_pipeline.py`
- `D:\proyectos\gi-ocr\backend\app\quality_gate.py`
- `D:\proyectos\gi-ocr\backend\app\templates\providers.py`
- `D:\proyectos\gi-ocr\scripts\benchmark_captura.py`
- `D:\proyectos\gi-ocr\backend\tests\test_quality_gate.py`
- `D:\proyectos\gi-ocr\backend\tests\test_benchmark_captura.py`
