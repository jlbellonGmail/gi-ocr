```yaml
status: approved
attempt: 1
feedback:
  - "No bloqueante: ningún umbral numérico por defecto queda fijado para las 8 señales (solo la de baja resolución tiene rangos concretos: 300x400 reject / 1200x1600 ok). Es aceptable dejarlo como decisión de implementación vía env var, pero conviene que docs/tecnica/calidad-captura-mobile.md registre explícitamente los valores por defecto elegidos y la justificación (aunque sea heurística, no calibrada contra fotos reales), para que quede auditable después."
  - "No bloqueante: al ser fixtures 100% sintéticas (por política del repo), el riesgo de umbrales mal calibrados contra fotos reales de celular queda abierto. El spec ya lo reconoce parcialmente en el caso borde de 'resolución baja por limitación real del dispositivo', pero valdría la pena que decision.md deje constancia de que la calibración fina de umbrales es un riesgo conocido a revisar con datos reales post-despliegue (no bloquea esta iteración)."
  - "No bloqueante: no se menciona accesibilidad del nuevo aviso `warn` (aria-live o equivalente) más allá de reutilizar `pill`/`error-box`. Dado que el propio scope explícitamente evita rediseño de UI, esto es aceptable, pero podría anotarse como fuera de alcance explícito igual que el resto."
```

## Análisis

**Regla dura de documentación (rechazo automático si falta):** cumplida. Criterios 1–5 exigen explícitamente `docs/tecnica/calidad-captura-mobile.md` (no vacío, con algoritmo/heurística por señal, umbrales y decisiones), `docs/usuario/calidad-captura-mobile.md` (no vacío, con ejemplo HTTP completo para los tres veredictos), `runs/06-calidad-captura-mobile/decision.md`, y enlaces exactos en `docs/tecnica/index.md` y `docs/usuario/index.md`. Verifiqué que ambos índices existen hoy con el patrón de lista `- [Título](slug.md)` que el spec debe replicar (`docs/tecnica/index.md`, `docs/usuario/index.md`), así que el criterio es ejecutable tal cual está redactado.

**Verificación de afirmaciones de código (no las di por ciertas, las leí):**
- `capture_pipeline.process_document` (líneas 209-219): abre imagen, `apply_exif_orientation`, convierte a RGB, `image_prep.prepare(...)`, luego `process_image(...)`. Coincide exactamente con la descripción del spec sobre dónde se inserta el nuevo chequeo (después de EXIF, antes de `prepare`/OCR).
- `image_prep.py`: `correct_perspective` (líneas 139-174) usa exactamente `cv2.Canny` + `cv2.findContours` + `cv2.approxPolyDP` como describe el spec para reutilizar en "documento cortado"/"mala perspectiva"/"encuadre insuficiente". `normalize_scale` (188-196) confirma que reduce el lado mayor a 1600px pero no sube resolución, sustentando la afirmación de "baja resolución" sobre la imagen original.
- `job_queue.py::_process` (120-153): confirma el flujo `queued → processing → ready/failed`, `run_in_executor(None, process_document, ...)`, y que `save_original` solo se llama en el camino `ready`. Esto valida técnicamente el criterio 18 (`confirm` debe seguir devolviendo 404 vía `store.original_exists` para el nuevo estado de rechazo, ya que si el builder no llama `save_original` en el camino `reject` —consistente con "no se generó resultado OCR"— el comportamiento de 404 se hereda gratis del código existente). También valida el criterio 19: `retry()` (líneas 80-92) solo bloquea estados `ready`/`confirmed`, por lo que un nuevo estado de rechazo por calidad seguiría siendo reintentable sin cambios adicionales, tal como exige el spec.
- `job_store.py`: `original_exists` chequea únicamente la existencia del archivo `jobs/{id}.json` escrito por `save_original`, independiente del estado en memoria — confirma que el diseño propuesto es coherente con la arquitectura real, no una suposición optimista.
- `upload_validation.py`: confirma el patrón `GI_OCR_MAX_UPLOAD_BYTES` (líneas 64-80) que el spec cita como precedente para umbrales globales vía env var — la analogía es válida.
- `backend/config/services.ini`: confirmado que es únicamente configuración de extracción de campos por servicio (`Field.*.Regex`, `Patterns`, etc.), sin ningún umbral de calidad de imagen. La decisión del spec de **no** tocar `services.ini` para los umbrales de calidad (por correr antes de conocer el proveedor) es técnicamente correcta y no viola el ADR-007; está bien razonada y declarada explícitamente como riesgo discutible, no asumida en silencio.
- `docs/tecnica/correccion-orientacion-exif.md` (líneas 229-230): confirma textualmente "Control de calidad de captura (blur, baja resolución, mala iluminación): es `06-calidad-captura-mobile`, no esta feature" — la cita de contexto del spec es exacta, no inventada.
- `ROADMAP.md` (líneas 130-134): el ítem `06` backlog coincide palabra por palabra con las 8 señales y los tres niveles de decisión que el spec dice cubrir. Alcance no se desvía del ítem de backlog.
- `docs/tecnica/captura-ocr-local-agil.md` (línea 33): confirma el dato "2.35–2.57 s por documento" citado en el spec como motivación de negocio — no es un número inventado.
- `frontend/src/app.js`: confirmado `pill`/`error-box` (líneas 102-103, 123, 141), `renderReady` (línea 129) accede a `res.processing_metadata` (línea 132) — punto de integración natural para `quality_gate`, y `stat-queued`/`stat-ready`/`stat-failed` (líneas 81-83) — confirma que el spec identificó correctamente que el nuevo estado debe contemplarse ahí para no contarse mal en el resumen.
- `backend/app/main.py`: `get_job`, `retry_job`, `confirm_job`, `download_confirmed`, `download_original` verificados línea por línea; ninguna afirmación del spec sobre comportamiento HTTP existente es falsa.

**8 señales de calidad (regla de dominio OCR):** cada una de las 8 (criterios 6-13: blur, baja resolución, reflejos, sombras, documento cortado, mala perspectiva, mala iluminación, encuadre insuficiente) declara explícitamente señal/métrica, fixture sintética concreta, salida esperada por umbral, y falso positivo evitado — cumple la regla no negociable de `AGENTS.md` sobre mejoras OCR, incluso siendo un chequeo pre-OCR genérico (no de campo específico), con una justificación razonable de por qué no aplica por documento/proveedor (corre antes de clasificar el proveedor).

**Criterios de aceptación verificables:** los criterios 14-19 son verificables por test concreto (mock/spy sobre `ocr_engine`, aserciones sobre `processing_metadata.quality_gate`, estado de job, comportamiento de `confirm`/`retry`). No encontré criterios vagos tipo "debe funcionar bien" sin métrica asociada.

**Casos borde:** cubren señales simultáneas, deduplicación de causa raíz (perspectiva vs. corte), imágenes degeneradas, imágenes corruptas vs. baja calidad válida, camino PDF explícitamente fuera de alcance (consistente con precedente documentado en `05`), determinismo de `retry`, y formatos no soportados. No encontré contradicciones entre "Casos borde" y "Criterios de aceptación" ni con "Riesgos/supuestos".

**Stack/dependencias:** no se introduce dependencia nueva (`opencv-python-headless` ya está en `backend/requirements.txt` y ya se usa en `image_prep.py` para operaciones equivalentes); no se propone JSON como config nueva; no se asume backend/BD no documentado. No amerita rechazo por el criterio de arquitectura no declarada.

**Contenido de negocio inventado:** no encontré datos, textos legales, precios ni certificaciones inventados; los mensajes de ejemplo citados (ej. "El documento está cortado en el encuadre") son ejemplos ilustrativos de identificador de razón, no contenido normativo fijo, y el spec dice explícitamente "a decidir el texto exacto en la implementación".

**Conclusión:** el spec está más verificado contra el código real de lo habitual — cada afirmación técnica citada fue confirmada leyendo el archivo correspondiente, no asumida. Cumple ambas reglas duras de documentación/decisión/índices, cumple la regla de dominio OCR para las 8 señales, no introduce stack ni dependencias no declaradas, no inventa contenido de negocio, y los criterios de aceptación son objetivamente verificables. Apruebo sin bloqueos; el feedback listado es de mejora opcional, no condiciones de rechazo.

Archivos relevantes leídos: `D:\proyectos\gi-ocr\runs\06-calidad-captura-mobile\spec.md`, `D:\proyectos\gi-ocr\backend\app\capture_pipeline.py`, `D:\proyectos\gi-ocr\backend\app\image_prep.py`, `D:\proyectos\gi-ocr\backend\app\job_queue.py`, `D:\proyectos\gi-ocr\backend\app\job_store.py`, `D:\proyectos\gi-ocr\backend\app\upload_validation.py`, `D:\proyectos\gi-ocr\backend\app\main.py`, `D:\proyectos\gi-ocr\backend\config\services.ini`, `D:\proyectos\gi-ocr\docs\tecnica\correccion-orientacion-exif.md`, `D:\proyectos\gi-ocr\docs\tecnica\captura-ocr-local-agil.md`, `D:\proyectos\gi-ocr\docs\tecnica\index.md`, `D:\proyectos\gi-ocr\docs\usuario\index.md`, `D:\proyectos\gi-ocr\frontend\src\app.js`, `D:\proyectos\gi-ocr\ROADMAP.md`.
