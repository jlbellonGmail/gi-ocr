```yaml
status: rejected
attempt: 1
feedback:
  - "GAP DE ALCANCE — 'anonimización' del ítem de roadmap no está cubierto en ningún criterio de aceptación ni en 'Explícitamente NO incluye', y el spec.md no menciona la palabra 'anonimiz' ni una vez (grep -i confirmado: cero resultados). El texto de ROADMAP.md ('extension, MIME, firma de archivo, tamaño maximo, nombres aleatorios, permisos, retencion, anonimizacion y redaccion de datos sensibles en logs') lista 'anonimización' como ítem propio, distinto de 'nombres aleatorios' y de 'redacción de datos sensibles en logs' — no es sinónimo de ninguno de los dos ya cubiertos. El spec resuelve explícitamente CADA otra ambigüedad del enunciado del roadmap en 'Riesgos/supuestos' (números de retención, exclusión de ready/, alcance del helper de redacción, no agregar dependencia para magic bytes) pero omite esta por completo, rompiendo su propio patrón. Interpretación razonable pendiente de decidir explícitamente: strippear metadata identificatoria embebida en la imagen (EXIF GPS/device info; confirmado que backend/app/capture_pipeline.py e image_prep.py hoy solo LEEN el tag Orientation para corregir rotación — runs/05-correccion-orientacion-exif/spec.md — y no eliminan ningún otro tag EXIF, así que una foto de celular subida hoy conserva GPS/modelo de dispositivo en output/uploads/ sin que nada de este spec lo toque). Corrección requerida: o se agrega un criterio de aceptación concreto y testeable para 'anonimización' (p. ej. stripping de EXIF sensible antes de persistir en output/uploads/, o enmascarado de PII en output/jobs|confirmed/), o se agrega una entrada explícita en 'Explícitamente NO incluye' con la misma calidad de justificación que las demás exclusiones del spec, dejando constancia de por qué se decide no cubrirlo en esta ronda."
  - "INCONSISTENCIA VERIFICADA CONTRA CÓDIGO — Criterio de aceptación 8 pide verificar que el marcador/ruta absoluta no aparece 'en job[\"error\"] devuelto por GET /api/v1/jobs/{job_id} ni en output/jobs/{job_id}.json'. Verificado en backend/app/job_queue.py (JobQueue._process): store.save_original(job_id, result) solo se llama dentro del bloque try, en el camino de éxito (línea 143); el bloque except (líneas 145-151, camino de job fallido) solo hace store.put(job) en memoria y NUNCA escribe output/jobs/{job_id}.json (confirmado también con grep de save_original en todo backend/app/: único call site). Es decir, para un job fallido ese archivo JSON simplemente no existe hoy. El test descrito en el criterio 8 no puede verificar 'que el marcador no aparece en ese archivo' porque el archivo no se crea para jobs failed — o el test miente (pasa trivialmente porque el archivo no existe, no porque la redacción funcione), o hay que corregir el criterio para no exigir esa aserción sobre un archivo que el pipeline actual no escribe en el camino de falla. Acción: reescribir el criterio 8 para que la verificación de archivo en disco sea sobre un caso real donde sí se persiste (o eliminar esa parte de la aserción y dejar solo la verificación vía GET /api/v1/jobs/{job_id}), sin introducir en silencio un cambio de comportamiento (persistir jobs failed a disco) que no está declarado como criterio ni discutido en 'Riesgos/supuestos'."
  - "CASO BORDE FALTANTE, relevante al dominio (formatos soportados) — El criterio 4 (contraste Content-Type declarado vs. familia de firma detectada) no contempla el caso de Content-Type ausente, vacío o genérico (application/octet-stream). Es un caso real, no exótico: los navegadores frecuentemente no logran inferir el MIME type de archivos .tif/.tiff (uno de los 4 tipos soportados según ALLOWED_EXTS/SUPPORTED) y el objeto File del <input type=file> puede reportar f.type === '' para ese formato, lo cual frontend/src/app.js reenvía tal cual vía FormData (fd.append('files', f, f.name), sin fijar Content-Type manualmente). Si el contraste Content-Type/magic-bytes se implementa de forma estricta sin manejar este caso, se corre el riesgo real de rechazar cargas legítimas de TIFF con un 400/415 falso positivo. Falta un criterio o caso borde explícito: qué hace el sistema cuando el Content-Type declarado está ausente/vacío/genérico (recomendado: no tratarlo como mismatch, confiar solo en la firma de archivo en ese caso)."
```

## Análisis

Auditoría de `runs/14-seguridad-privacidad-documentos/spec.md` contra el código real (`backend/app/main.py`, `job_store.py`, `job_queue.py`, `storage_bridge_writer.py`, `inbound_watcher.py`, `validators.py`, `backend/tests/test_security.py`, `backend/tests/test_api_jobs.py`, `backend/requirements.txt`, `docs/tecnica/arquitectura.md` ADR-001/006/007/009, `docs/tecnica/index.md`, `docs/usuario/index.md`, `ROADMAP.md`, `.github/workflows/ci.yml`).

**Lo que está bien y verificado como correcto:**
- Todas las afirmaciones sobre el estado actual del código son exactas: extensión/tamaño hardcodeado ya validados (`_validate_upload`, `MAX_BYTES = 30MB`), no hay validación de Content-Type ni magic bytes, `_save_upload` usa `{timestamp}_{nombre_sanitizado}` como nombre (predecible, conserva PII en la ruta), no hay ningún `os.chmod` en el proyecto, no hay purga/expiración en ningún directorio de estado, no hay logging estructurado, `requirements.txt` no tiene `python-magic`/`filetype`, `test_upload_rejects_bad_ext` existe, el patrón `skipif` con motivo ya se usa en el repo, y CI corre en `ubuntu-latest`.
- Cumple el requisito automático de AGENTS.md: exige explícitamente `docs/tecnica/seguridad-privacidad-documentos.md`, `docs/usuario/seguridad-privacidad-documentos.md`, `decision.md` y enlaces exactos en ambos índices con mismo título.
- La convención de nombrado de docs (slug sin número) coincide con los índices reales.
- Las exclusiones declaradas están bien justificadas contra ADR-001/006/007/009 reales, y no reabren esos ADR.
- Los criterios de aceptación son en general concretos y verificables, y los números de retención por defecto (7/90/30 días) tienen justificación razonable.

**Motivo del rechazo:** tres puntos accionables en el YAML — el más grave es que "anonimización", pedida explícitamente y por separado en el ítem de roadmap 14, no aparece ni una vez en el spec (ni implementada ni excluida con justificación). Los otros dos: inconsistencia verificada entre el criterio 8 y el comportamiento real de `JobQueue._process` (no persiste jobs fallidos a disco), y caso borde de dominio faltante sobre Content-Type ausente/genérico para TIFF.
