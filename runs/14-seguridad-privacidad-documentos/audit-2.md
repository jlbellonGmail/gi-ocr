```yaml
status: rejected
attempt: 2
feedback:
  - "Los 3 puntos de audit-1 SÍ están resueltos correctamente, verificado contra código real: (a) el criterio 9 (anonimización EXIF) cubre el gap de 'anonimización' del roadmap con un criterio concreto y testeable, coherente con el contexto verificado (grep de exif sobre backend/app/ = cero resultados en image_prep.py/capture_pipeline.py; Pillow>=10.2.0 ya está en backend/requirements.txt); (b) el criterio 8 ahora solo exige verificación vía GET /api/v1/jobs/{job_id}, coherente con job_queue.py::_process líneas 138-151 (store.save_original solo se invoca en el try/éxito, línea 143; el except/failed solo hace store.put en memoria, nunca escribe a disco — confirmado); (c) el caso borde de Content-Type ausente/genérico para TIFF está bien resuelto en el criterio 4 y en 'Casos borde', y no abre ningún hueco de seguridad porque la validación de firma (criterio 1) es independiente y siempre se aplica — el contraste Content-Type/firma es una capa adicional, no un reemplazo."
  - "PROBLEMA NUEVO introducido por el criterio 9 (no presente en versiones previas del spec) — riesgo de degradación de calidad de imagen no abordado, y es precisamente el archivo que consume el pipeline OCR: para stripping de EXIF en JPEG sin agregar dependencia nueva, la única vía con Pillow es decodificar y re-guardar (Image.open + Image.save), y el default de calidad de guardado JPEG en Pillow es quality=75 — sensiblemente menor que la calidad típica de una foto de celular (85-95). El archivo resultante en output/uploads/ (con EXIF ya stripeado) es el MISMO que job_queue.py::_process pasa a capture_pipeline.process_document vía job['file_path'] para hacer OCR (confirmado: _save_upload en main.py escribe a output/uploads/<random>_<name>, y ese path es el que se enqueue()a y luego se procesa). Es decir: la decisión ya tomada en 'Riesgos/supuestos' ('Pillow es suficiente, sin agregar dependencia nueva') tiene una consecuencia no declarada — riesgo real de degradar la imagen fuente de la que depende toda la extracción de campos del sistema — y el spec no dice nada al respecto, rompiendo el patrón que el propio spec sigue en el resto de 'Riesgos/supuestos' (donde cada trade-off se declara y justifica explícitamente). Esto encaja directamente con la regla de dominio de AGENTS.md sobre declarar 'validación aplicada' para cualquier cosa que toque el archivo que el OCR procesa. Corrección requerida: agregar un criterio o una entrada en 'Riesgos/supuestos' que resuelva esto explícitamente — p. ej. exigir `quality=\"keep\"` (feature de Pillow que reutiliza las tablas de cuantización JPEG originales al re-guardar, evitando recompresión con pérdida) o un `quality` alto explícito, o aceptar y acotar la degradación con justificación, con un test que lo verifique (no alcanza con confirmar solo ausencia de tags EXIF)."
  - "PROBLEMA NUEVO / gap de cobertura de test, mismo patrón de rigor que audit-1 ya exigió para TIFF en el criterio 4 — el criterio 9 declara explícitamente que aplica a 'JPEG, TIFF' por igual ('los únicos de los 4 tipos soportados con bloque EXIF estándar'), pero el 'Test:' descrito solo cubre una fixture JPEG ('subir una imagen JPEG fixture con EXIF GPSInfo, Make, Model y Orientation=6'). No hay fixture ni test TIFF para el mismo criterio, pese a que el manejo de EXIF de Pillow para TIFF tiene diferencias técnicas reales frente a JPEG (los tags EXIF en TIFF conviven con la IFD nativa del formato, y el comportamiento de `Image.getexif()`/soporte de `GPSInfo` en TIFF no está garantizado idéntico al de JPEG). Esto deja abierta la posibilidad de que el criterio 10 ('cubre los criterios... 9 con tests nuevos') pase en verde sin que la rama TIFF de la anonimización EXIF esté siquiera ejercitada. Corrección requerida: agregar un test/fixture TIFF explícito al criterio 9 (mismo nivel de detalle que el JPEG), o acotar el texto del criterio para declarar TIFF como fuera de alcance de esta ronda con justificación en 'Riesgos/supuestos', igual que se hizo con otras exclusiones del spec."
```

## Análisis

Auditoría de `runs/14-seguridad-privacidad-documentos/spec.md` (v2) contra código real: `backend/app/main.py` (`_validate_upload`, `_save_upload`, `create_jobs`, `ALLOWED_EXTS`, `MAX_BYTES`), `backend/app/job_queue.py` (`JobQueue._process`), `backend/app/job_store.py` (`JobStore`, `sanitize_name`), `backend/app/image_prep.py`, `backend/app/capture_pipeline.py`, `backend/app/inbound_watcher.py` (`SUPPORTED`, `InboundWatcher._seen`), `backend/requirements.txt`, y `runs/14-seguridad-privacidad-documentos/audit-1.md`.

**Los 3 puntos de audit-1 están resueltos correctamente y de forma verificable, no solo mencionados en prosa** — ver detalle en el YAML. En particular se verificó directamente en código que:
- `image_prep.py` no lee ni escribe ningún tag EXIF (solo hace corrección geométrica heurística vía OpenCV sobre el array ya decodificado); `capture_pipeline.py` abre la imagen con `Image.open(path).convert("RGB")` sin tocar EXIF. La afirmación de contexto del spec es exacta.
- `job_queue.py::_process`: `store.save_original(job_id, result)` (línea 143) solo está en el camino `try`/éxito; el bloque `except` (líneas 145-151) solo hace `store.put(job)` en memoria. El ajuste del criterio 8 (solo `GET /api/v1/jobs/{job_id}`) es coherente con el comportamiento real.
- El contraste Content-Type/firma no abre ningún hueco: la validación de firma (criterio 1) es independiente y siempre obligatoria, así que "no tratar Content-Type ausente como mismatch" no reemplaza ni debilita esa capa.

**Lo demás del spec está sólido:** cumple el requisito automático de AGENTS.md (`docs/tecnica/`, `docs/usuario/`, `decision.md`, índices — criterios 11-13), las exclusiones en "Explícitamente NO incluye" siguen bien justificadas y coherentes con el nuevo criterio 9 (distingue correctamente metadata EXIF incidental vs. contenido de negocio OCR), los números de retención y el alcance del helper de redacción siguen igual de bien fundamentados que en audit-1, y los casos borde de dominio (lote parcial, `InboundWatcher._seen` indexado por nombre+hash, retención en 0/negativa, Windows skip) siguen siendo correctos contra código real.

**Motivo del rechazo:** el propio criterio 9 (agregado para resolver el gap de "anonimización" de audit-1) introduce dos problemas nuevos, no presentes en la versión anterior del spec: (1) riesgo no declarado de degradación de calidad de imagen al re-guardar JPEG con Pillow para stripping de EXIF — justo el archivo que consume el pipeline OCR — sin ningún criterio ni decisión que lo aborde; (2) el criterio declara cobertura JPEG+TIFF pero el test descrito solo ejercita JPEG, dejando la rama TIFF sin fixture ni validación, repitiendo el mismo tipo de gap de dominio que audit-1 ya señaló para Content-Type/TIFF.
