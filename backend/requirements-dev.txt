# Dependencias de desarrollo / testing locales (no requeridas en runtime).
# pytest y httpx ya llegan fijados (==) via -r requirements.txt (ver
# backend/requirements.txt); no se repiten acá para no duplicar pines.
-r requirements.txt

# Herramientas de calidad de CI (feature 18-calidad-ci-supply-chain):
# lint + formato, chequeo de tipos y auditoría de vulnerabilidades.
ruff==0.16.4
mypy==2.3.1
pip-audit==2.10.1

# Playwright pertenece al scaffolding de la feature 17-pruebas-e2e-mobile-real
# (aun [ ] pendiente en ROADMAP.md). No se fija su version en esta feature:
# esa decision (incluida la version de navegadores compatible) le corresponde
# a la feature 17 cuando se implemente.
playwright>=1.40.0